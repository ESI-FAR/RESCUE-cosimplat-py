<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="RESCUE co-simulation platform dashboard">
        <meta name="keywords" content="RESCUE, co-simulation, dashboard">
        {% block head %}
            <title>
                {% block title %}
                {% endblock title %}
            - RESCUE co-simulation platform dashboard</title>
        {% endblock head %}
        <script src="{{ url_for('static', filename='htmx.js') }}"></script>
        <style>
            body {
                margin: 0;
                padding: 0;
                background: #f8f9fa;
            }
            .header-bg {
                background: #ddd;
                padding: 1.2rem;
                box-sizing: border-box;
            }
            .header-bg a {
                color: inherit;
                text-decoration: none;
            }
            .btn {
                padding: 0.6em 1.4em;
                font-size: 1.1em;
                font-weight: 600;
                color: #fff;
                background: #0074d9;
                border: none;
                border-radius: 6px;
                box-shadow: 0 2px 8px rgba(0,0,0,0.07);
                cursor: pointer;
            }
            .btn:hover, .btn:focus {
                background: #005fa3;
            }
            #content {
                padding: 1.2rem;
            }
        </style>
    </head>
    <body>
        <div class="header-bg">
            <h1 style="margin: 0;">
                <a href="{{ url_for('index') }}">RESCUE co-simulation platform dashboard</a>
            </h1>
        </div>
        <div id="content">
            {% block content %}
            {% endblock content %}
        </div>
    </body>
</html>
